# List of flatpaks to auto-install on the next boot (if they have not
# already been uninstalled).
flatpaksdir = $(datadir)/eos-application-tools/flatpak-autoinstall.d

dist_flatpaks_DATA = \
	50-loupe.json \
	50-music.json \
	$(NULL)
